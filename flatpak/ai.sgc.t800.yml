app-id: ai.sgc.t800
runtime: org.freedesktop.Platform
runtime-version: "24.08"
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "24.08"
command: run.sh
separate-locales: false

finish-args:
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --share=ipc
  - --device=dri
  - --filesystem=home
  - --socket=ssh-auth
  - --socket=session-bus
  - --talk-name=org.freedesktop.secrets
  - --env=ELECTRON_TRASH=gio
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons
  - --env=ELECTRON_OZONE_PLATFORM_HINT=auto

modules:
  - name: t800
    buildsystem: simple
    build-commands:
      - chmod +x t800.AppImage
      - ./t800.AppImage --appimage-extract

      - install -Dm755 squashfs-root/t800 /app/bin/t800
      - cp -r squashfs-root/resources /app/bin/
      - cp -r squashfs-root/locales /app/bin/ || true

      - cp squashfs-root/*.so /app/bin/ || true
      - cp squashfs-root/*.pak /app/bin/ || true
      - cp squashfs-root/*.bin /app/bin/ || true
      - cp squashfs-root/*.dat /app/bin/ || true
      - cp squashfs-root/*.json /app/bin/ || true

      - |
        cat > run.sh << 'EOF'
        #!/bin/bash
        export TMPDIR="$XDG_RUNTIME_DIR/app/$FLATPAK_ID"
        exec zypak-wrapper /app/bin/t800 "$@"
        EOF
      - chmod +x run.sh
      - install -Dm755 run.sh /app/bin/run.sh

      - install -Dm644 ai.sgc.t800.desktop /app/share/applications/ai.sgc.t800.desktop

      - install -Dm644 ai.sgc.t800.metainfo.xml /app/share/metainfo/ai.sgc.t800.metainfo.xml

      - install -Dm644 ai.sgc.t800.svg /app/share/icons/hicolor/scalable/apps/ai.sgc.t800.svg
      - install -Dm644 icon-256.png /app/share/icons/hicolor/256x256/apps/ai.sgc.t800.png || true
      - install -Dm644 icon-128.png /app/share/icons/hicolor/128x128/apps/ai.sgc.t800.png || true

    sources:
      - type: file
        url: https://github.com/P3X-118/T-800/releases/download/release-VERSION_PLACEHOLDER-tag/t800_linux_x64_appimage.AppImage
        sha256: CHECKSUM_X64_PLACEHOLDER
        dest-filename: t800.AppImage
        only-arches:
          - x86_64

      - type: file
        url: https://github.com/P3X-118/T-800/releases/download/release-VERSION_PLACEHOLDER-tag/t800_linux_arm64_appimage.AppImage
        sha256: CHECKSUM_ARM64_PLACEHOLDER
        dest-filename: t800.AppImage
        only-arches:
          - aarch64

      - type: file
        path: ai.sgc.t800.desktop

      - type: file
        path: ai.sgc.t800.metainfo.xml

      - type: file
        path: ai.sgc.t800.svg

      - type: file
        path: icon-256.png

      - type: file
        path: icon-128.png
